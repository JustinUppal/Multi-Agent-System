[project]
name = "jpm-takehome"
version = "0.1.0"
description = "A multi-agent financial advisory system built on Google ADK"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "google-adk>=1.0.0",
    "python-dotenv>=1.0.0",
]
